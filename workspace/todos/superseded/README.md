# Todos — superseded

Old `plan.md` files replaced by a newer plan. Kept for history.

When `/todos` regenerates a plan, the old `plan.md` is moved here with a date suffix:
`plan-YYYY-MM-DD.md`. Do not delete these files.
