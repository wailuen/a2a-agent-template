# Todos — active

Wave files currently being implemented. Each file: `w[NNN]-<title>.md`.

Populated by `/todos`. Consumed by `/wave` and `/implement`.
A wave file moves to `completed/` after a clean archive, or to `deferred/` if blocked.
